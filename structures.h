#include <string>

struct {
    std::string name;
    std::string workerSince;
} Worker;

struct {
    std::string name;
    std::string dateSince;
    float value;
} Item;

struct {
    int sectionX;
    int sectionY;
    int sectionZ;
} Shelf;